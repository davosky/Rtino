<h3>
  <%= inline_svg('structure_icon.svg', size: '32px * 32px', preserve_aspect_ratio: 'xMaxYMax meet') %>
  &nbsp;<span class='text-primary'>Strutture</span>
  <span class='pull-right'>
    <h6>
      <% if admin? %>
        <%= link_to new_structure_path do %>
          <%= inline_svg('structure_icon.svg', size: '18px * 18px', preserve_aspect_ratio: 'xMaxYMax meet') %>
          Nuova Struttura
        <% end %>
      <% end %>
    </h6>
  </span>
</h3>
<hr class='hr-primary'>
<br>
<div class='panel panel-default'>
  <div class='panel-body'>
    <table class='table table-hover'>
      <thead>
        <tr>
          <td>Nome</td>
          <td>Posizione</td>
        </tr>
      </thead>
      <tbody>
        <% @structures.each do |structure| %>
          <tr>
            <td class='text-primary'><%= structure.name %></td>
            <td class='text-primary'><%= structure.position %></td>
            <td>
              <%= link_to edit_structure_path(structure), title: 'Modifica la Struttura Corrente', rel: 'tooltip' do %>
                <i class='fas fa-pencil-alt text-warning'></i>
              <% end %>
            </td>
            <td>
              <%= link_to structure, method: :delete, data: { confirm: 'Sei sicura/o di voler eliminare la Struttura corrente?' }, title: 'Elimina ls Struttura Corrente', rel: 'tooltip' do %>
                <i class='fas fa-trash-alt text-danger'></i>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
    <span class='text-centered'>
      <%= will_paginate @structures, previous_label: 'Precedente', next_label: 'Prossima', renderer: BootstrapPagination::Rails %>
    </span>
  </div>
</div>
